<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CaptureImage.ViewModels.Settings;assembly=CaptureImage.ViewModels"
             xmlns:sys="clr-namespace:System.Globalization;assembly=System.Runtime"
             xmlns:models="clr-namespace:CaptureImage.Core.Models;assembly=CaptureImage.Core"
             mc:Ignorable="d"
             x:Class="CaptureImage.UI.Views.SettingsView"
             x:DataType="vm:SettingsViewModel">

    <ScrollViewer>
        <StackPanel Margin="24,16" Spacing="18" MaxWidth="620">

            <TextBlock Text="{Binding Localization[Settings_Title]}" FontSize="24" FontWeight="SemiBold" />

            <!-- Language -->
            <StackPanel Spacing="4">
                <TextBlock Text="{Binding Localization[Settings_Language]}" Opacity="0.7" FontSize="12" />
                <ComboBox ItemsSource="{Binding SupportedCultures}"
                          SelectedItem="{Binding SelectedCulture, Mode=TwoWay}"
                          HorizontalAlignment="Stretch">
                    <ComboBox.ItemTemplate>
                        <DataTemplate DataType="sys:CultureInfo">
                            <TextBlock Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <!-- Hotkey -->
            <StackPanel Spacing="4">
                <TextBlock Text="{Binding Localization[Settings_Hotkey]}" Opacity="0.7" FontSize="12" />
                <TextBlock Text="Ctrl+Shift+F12" FontFamily="Consolas, Courier New, monospace" />
                <TextBlock Text="{Binding Localization[Settings_HotkeyHint]}" FontSize="11" Opacity="0.5" />
            </StackPanel>

            <!-- Default format -->
            <StackPanel Spacing="4">
                <TextBlock Text="{Binding Localization[Settings_Format]}" Opacity="0.7" FontSize="12" />
                <ComboBox ItemsSource="{Binding SupportedFormats}"
                          SelectedItem="{Binding DefaultFormat, Mode=TwoWay}"
                          HorizontalAlignment="Stretch" />
            </StackPanel>

            <!-- Quality sliders -->
            <Grid ColumnDefinitions="*,16,*">
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="{Binding Localization[Settings_JpegQuality]}" Opacity="0.7" FontSize="12" />
                    <Slider Minimum="1" Maximum="100" Value="{Binding JpegQuality, Mode=TwoWay}" TickFrequency="10" />
                    <TextBlock Text="{Binding JpegQuality}" FontSize="11" Opacity="0.6" HorizontalAlignment="Right" />
                </StackPanel>
                <StackPanel Grid.Column="2" Spacing="4">
                    <TextBlock Text="{Binding Localization[Settings_WebpQuality]}" Opacity="0.7" FontSize="12" />
                    <Slider Minimum="1" Maximum="100" Value="{Binding WebpQuality, Mode=TwoWay}" TickFrequency="10" />
                    <TextBlock Text="{Binding WebpQuality}" FontSize="11" Opacity="0.6" HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>

            <!-- Output folder -->
            <StackPanel Spacing="4">
                <TextBlock Text="{Binding Localization[Settings_OutputFolder]}" Opacity="0.7" FontSize="12" />
                <TextBox Text="{Binding OutputDirectory, Mode=TwoWay}"
                         Watermark="{Binding Localization[Settings_OutputFolderDefault]}" />
            </StackPanel>

            <!-- File name template -->
            <StackPanel Spacing="4">
                <TextBlock Text="{Binding Localization[Settings_FileNameTemplate]}" Opacity="0.7" FontSize="12" />
                <TextBox Text="{Binding FileNameTemplate, Mode=TwoWay}"
                         FontFamily="Consolas, Courier New, monospace" />
            </StackPanel>

            <!-- Toggles -->
            <StackPanel Spacing="6">
                <CheckBox Content="{Binding Localization[Settings_PreviewBeforeSave]}"
                          IsChecked="{Binding PreviewBeforeSave, Mode=TwoWay}" />
                <CheckBox Content="{Binding Localization[Settings_MinimizeToTray]}"
                          IsChecked="{Binding MinimizeToTray, Mode=TwoWay}" />
                <CheckBox Content="{Binding Localization[Settings_SoundEnabled]}"
                          IsChecked="{Binding SoundEnabled, Mode=TwoWay}" />
            </StackPanel>

            <!-- Import / export / open -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Name="ImportButton" Content="{Binding Localization[Settings_Import]}" />
                <Button Name="ExportButton" Content="{Binding Localization[Settings_Export]}" />
                <Button Content="{Binding Localization[Settings_OpenFile]}"
                        Command="{Binding OpenSettingsFileCommand}" />
            </StackPanel>

            <TextBlock Text="{Binding SettingsFilePath}" Opacity="0.4" FontSize="11" TextWrapping="Wrap" />

        </StackPanel>
    </ScrollViewer>

</UserControl>
