<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CaptureImage.UI.Views.PinnedThumbnailWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="Root" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Drag-handle title bar. Mica + ExtendsContentIntoTitleBar already makes the
             top strip draggable; the buttons sit on top with HitTestVisible. -->
        <Grid Grid.Row="0"
              Height="32"
              ColumnSpacing="4"
              Padding="8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       x:Name="TitleText"
                       VerticalAlignment="Center"
                       FontSize="{StaticResource FontSizeCaption}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextTrimming="CharacterEllipsis" />
            <Button Grid.Column="1"
                    x:Name="OpenFileButton"
                    Click="OnOpenFileClick"
                    ToolTipService.ToolTip="{Binding Localization[PinnedWindow_Open]}"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Padding="6,2">
                <FontIcon Glyph="&#xE8A7;" FontSize="14" />
            </Button>
            <Button Grid.Column="2"
                    x:Name="CloseButton"
                    Click="OnCloseClick"
                    ToolTipService.ToolTip="{Binding Localization[PinnedWindow_Close]}"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Padding="6,2">
                <FontIcon Glyph="&#xE10A;" FontSize="12" />
            </Button>
        </Grid>

        <!-- Thumbnail image -->
        <Border Grid.Row="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1,1,1,0"
                CornerRadius="0"
                Background="#111111">
            <Image x:Name="ThumbnailImage"
                   Stretch="Uniform"
                   Margin="0" />
        </Border>
    </Grid>
</Window>
